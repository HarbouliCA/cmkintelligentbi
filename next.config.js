/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir is now enabled by default in Next.js 13 and 14
  // No need to specify it here unless it's still required for your version
};

module.exports = nextConfig;
